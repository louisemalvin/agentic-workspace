# Project Init Mode

Use when the repository does not have a project-local `AGENTS.md` file.

## Rules

- Run `agent-init` to generate the project-local `AGENTS.md`.
- Immediately after initialization, fill out the project-local `AGENTS.md` by inspecting the repository and interviewing the user on open questions.
- Do not perform implementation work until the project guide is initialized.
