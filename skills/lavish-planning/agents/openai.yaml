interface:
  display_name: "Lavish Planning"
  short_description: "Use Lavish for visual planning and write decisions back to .tasks."
  default_prompt: "Use $lavish-planning to review this plan visually, then update the active task artifact."
policy:
  allow_implicit_invocation: true
