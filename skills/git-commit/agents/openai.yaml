interface:
  display_name: "Git Commit"
  short_description: "Create clean Conventional Commits"
  default_prompt: "Use $git-commit to inspect the current diff and create a clean Conventional Commit."
